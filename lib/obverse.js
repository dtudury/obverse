const {
    toType,
    store,
    referencers,
    reference,
    dereference,
    BOOLEAN,
    NULL,
    UNDEFINED,
    NUMBER,
    STRING,
    SYMBOL
} = require("./primitiveStore");


console.log(SYMBOL);
